{
  "name": "bunchive",
  "module": "index.ts",
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "bu": "bun run ./src/cli.ts",
    "build": "bun build ./src/cli.ts --target bun --minify --outfile bu.js",
    "test": "bun test",
    "format": "oxfmt src"
  },
  "dependencies": {
    "node-cron": "^4.2.1",
    "tar-stream": "^3.1.7"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/tar-stream": "^3.1.4",
    "oxfmt": "^0.21.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
